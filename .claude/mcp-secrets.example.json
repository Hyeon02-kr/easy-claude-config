{
  "$comment": "MCP 서버 비밀값 양식 예시. 이 파일은 커밋되며 실제 값을 넣지 않는다. 복사해서 같은 디렉터리에 mcp-secrets.json 으로 만들고 거기에 실제 값을 채운다. mcp-secrets.json 은 .gitignore 로 막혀 있다.",
  "$format": "secrets 의 키는 mcp-servers.json 의 servers[].name 과 일치해야 한다. env 는 stdio 서버에, headers 는 http 서버에 쓴다. 둘 다 선택이며, 비밀값이 없는 서버는 아예 적지 않는다.",
  "secrets": {
    "context7": {
      "env": {
        "CONTEXT7_API_KEY": "PLACEHOLDER_실제값을_이_파일이_아니라_mcp-secrets.json_에_넣는다"
      }
    },
    "예시-http-서버": {
      "headers": {
        "Authorization": "Bearer PLACEHOLDER_실제값을_이_파일이_아니라_mcp-secrets.json_에_넣는다"
      }
    }
  }
}
